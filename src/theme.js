import { createTheme } from '@material-ui/core/styles';

export default createTheme({
    palette: {
        type: 'dark',
        
        primary: {
            main: '#262f34',
        },
        secondary: {
            main: '#f7803c'
        },

    },
    sidebarWidth: 260,
    sidebarMobileHeight: 90,
});
